<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.lance</groupId>
        <artifactId>agent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>testAllModel</artifactId>
    <packaging>jar</packaging>

    <name>testAllModel</name>
    <description>聚合启动模块：整合 web + mybatis，对外提供 REST CRUD</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>17</java.version>
    </properties>

    <dependencies>
        <!-- 内部模块：Web 层 + 持久层 -->
        <dependency>
            <groupId>com.lance</groupId>
            <artifactId>web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lance</groupId>
            <artifactId>mybatis</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lance</groupId>
            <artifactId>common</artifactId>
        </dependency>
        <!-- Spring Boot 启动（web 已含 starter-web，此处补全核心 starter） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <!-- 本地开发默认 H2；可切换 mysql / postgresql profile（PG 驱动由 mybatis 模块传递） -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Redis 连接测试（实验 6 前置） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.lance.testall.TestAllModelApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
